logging:
    level:
        web: DEBUG
auth0:
    audience: http://localhost:8081/
server:
    port: 8081
spring:
    datasource:
        driver-class-name: org.mariadb.jdbc.Driver
        url: jdbc:mariadb://localhost:49155/list
        username: root
        password: 46le54si
    h2:
        console:
            enabled: true
    jpa:
        database: mysql
        hibernate:
            ddl-auto: update
    security:
        oauth2:
            resourceserver:
                jwt:
                    issuer-uri: https://dev-6iexu8k3.us.auth0.com/